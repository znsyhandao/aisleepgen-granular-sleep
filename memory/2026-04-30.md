# 2026-04-30 粒计算项目日志

## 管道迭代

### 17:00-18:00 认知注入管道 v1 → v2
- **v1** (`cognition_inject.py`): 锚点扫描+认知记录+代码注入标记，commit `fa38092`
- **v1** (`cognition_pipeline.py`): 评估模板(价值/工时/风险)、备份机制(`backups/`)、回退(`rollback <cid>`)
- **v2** (`cognition_pipeline.py`): `optimize`模式——自主扫描待办→评估→选任务→备份→改代码→验证→记录
- 验证演示：输入"评分模型输出用叙事代替数字" → 管道走完，`generate_narrative()` 注入sleep_metrics.py

### 20:30-21:00 L3 思维轨迹进化框架
- **cognition_pipeline.py** 新增 `cmd_evolve()` + `evolve` CLI 命令
- 三阶段：并行方案生成 → 并行验证 → 选优+残差重组
- `c28f28a` feat: SWA权重重构(效率30→20,新增卸载速度10分)
- `e296b66` feat: 认知管道v2(自迭代循环)
- `fc78c85` feat: 叙事输出(评分+自然语言叙述)

### 21:00-21:23 L3 N1识别进化测试
- **目标**: N1 recall 22.9% → 50%+
- **3方案**: A(N1过渡区Boost), B(过渡期检测器), C(混合策略)
- **结果**: 方案B胜出 — 过渡检测(W→N2边界插入) + N1保护(防止平滑吞掉)
- **指标**: N1 recall 2.4% → 9.8%(单被试SC4001)
- **commit**: `6f7c506` feat: L3 N1进化(3方案对比+残差重组+N1保护)
- **根因发现**: N1在30秒epoch中只占10-15秒，特征空间与W完全重叠 → 物理极限，需要子epoch细分

## 关键决策
1. **L2 optimize模式完整闭环**: 评估→选择→执行→验证→记录，AI自主完成
2. **L3 evolve模式**: 多方案并行生成+验证+残差重组，超出单方案优化上限
3. **N1保护机制**: granular_stager.py中N1不参与平滑，防止过渡期被邻域分类吞掉
4. **N1子epoch方向**: 30秒→10秒级分析是唯一提升路径

## 当前代码状态
- `granular/transition_detector.py` (v4): 过渡期检测器，仅过渡检测+过预测抑制
- `granular/explainable_classifier.py`: 含N1过渡区Boost（失效但保留作为对照）
- `granular/granular_stager.py`: 含N1保护（N1不参与平滑）
- `cognition_pipeline.py`: v2自迭代 + L3 evolve模式
- `experiments/text_l3_compare.py`: 3方案对比测试脚本

## 已知问题
- ExplainableFuzzyClassifier.predict() 的 'stages' 局部变量问题导致方案A/C跑不通（代码注入但未修复）
- `2>/dev/null` 在PowerShell下不生效（改用 `$null` 或 `2>$null`）
